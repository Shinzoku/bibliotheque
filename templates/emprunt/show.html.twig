{% extends 'base.html.twig' %}

{% block title %}Détails Emprunt{% endblock %}

{% block body %}
    <h1 class="text-center m-5">Emprunt</h1>
    <div class="container-fluid d-flex flex-column align-items-center">
        <table class="table table-hover w-50">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ emprunt.id }}</td>
                </tr>
                <tr>
                    <th>Date_emprunt</th>
                    <td>{{ emprunt.dateEmprunt ? emprunt.dateEmprunt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Date_retour</th>
                    <td>{{ emprunt.dateRetour ? emprunt.dateRetour|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
            </tbody>
        </table>
        <div class="d-flex">
            <a href="{{ path('app_emprunt_index') }}" class="btn btn-outline-info">back to list</a>
            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('app_emprunt_edit', {'id': emprunt.id}) }}" class="btn btn-outline-dark">edit</a>

                {{ include('emprunt/_delete_form.html.twig') }}
            {% endif %}
        </div>
    </div>

{% endblock %}
